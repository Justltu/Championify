# Dependencies that are preappened to Championify files.

https = require('follow-redirects').https
http = require('follow-redirects').http
parseURL = require('url').parse
mkdirp = require 'mkdirp'
glob = require 'glob'
fs = require 'fs-extra'
